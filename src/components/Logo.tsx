const Logo = ({ className = '' }: { className?: string }) => (
  <span className={`font-serif text-xl tracking-[0.2em] ${className}`} aria-label="INVERSIA">
    INVERSIA
  </span>
);

export default Logo;
